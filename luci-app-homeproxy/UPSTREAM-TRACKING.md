# HomeProxy 上游核对记录

## 审查基线

- 来源：https://github.com/VIKINGYFY/packages.git
- 独立维护起点：上游 `3535ade`，本地同步提交 `a2f2057`；两者的 `luci-app-homeproxy` 目录树完全一致。
- 基线版本：`20260913-r1`。
- 仅选择性核对，不以两棵树的差异认定上游新增或删除了功能。

## 最新核对

- 核对时间：2026-09-14 22:21 HKT
- 上游最新提交：`1acb468`
- 当前本地版本：`20260914-r7`

## 吸收更新

- 上游 `1acb468` / `20260914-r1`：订阅更新移除当前普通主节点时，自动切换到首个可用节点；不存在可用节点时设为 `nil`。本地在 `20260914-r7` 吸收，并增加回归测试。

## 忽略更新

- 上游 `3535ade` / `20260913-r1`：曾同步后回退。关闭 IPv6 时，上游使用 DNS `prefer_ipv4` 并保留 IPv6 TUN 地址；本地 `c78187a` 改回 DNS `ipv4_only` 与仅 IPv4 TUN，避免无 IPv6 出口节点的 DNS 和 TUN 初始化问题。
- 上游 `10393a5` / `20260913-r1`：仅调整 PO 折行，无功能或翻译内容变化；不同步。
- 上游 `1acb468` / `20260914-r1`：版本号不直接同步；本地版本仅随实际修改递增。

## 自主修改

- `20260914-r1`：资源独立更新：固定提交下载、完整性与规则集解码校验、失败保留；Dashboard 不预置，仅支持独立安装、更新和移除。
- `20260914-r2`：分离手动、定时和单资源更新；Dashboard 仅在已安装时随手动更新。
- `20260914-r3`：资源应用失败时返回真实回滚状态。
- `20260914-r4`：强化订阅解析、端口初始化、reload 恢复和节点测速，并补齐回归测试；URL 参数仅解码一次，避免将 `/api?token=a%2Fb` 误改为 `/api?token=a/b`，覆盖 Host、密码及 HTTP、HTTP Upgrade、WebSocket 路径。
- `20260914-r5`：完善通用 OpenWrt 兼容：procd 接口触发、动态上行 DNS 发现和 sing-box TUN 栈选择。
- `20260914-r6`：收敛 LuCI 生命周期与响应式 UI，保持原生 LuCI/Aurora 风格。
- `20260914-r7`：补齐服务状态播报语义，吸收普通主节点失效回退修复，并更新上游核对记录。
