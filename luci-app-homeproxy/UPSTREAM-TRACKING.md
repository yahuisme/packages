# HomeProxy 上游核对记录

## 审查基线

- 来源：https://github.com/VIKINGYFY/packages.git
- 独立维护起点：上游 `3535ade`，本地同步提交 `a2f2057`；两者的 `luci-app-homeproxy` 目录树完全一致。
- 基线版本：`20260913-r1`。
- 上游 `3535ade` 曾同步，但本地随后在 `c78187a` 放弃其 IPv6 关闭时的 `prefer_ipv4` DNS 策略和双栈 TUN 地址：关闭 IPv6 时恢复为 DNS `ipv4_only` 与仅 IPv4 TUN 地址。独立维护以该回退后的状态开始。
- 仅选择性核对，不以两棵树的差异认定上游新增或删除了功能。

## 最新核对

- 核对时间：2026-09-14 22:21 HKT
- 上游最新提交：`1acb468`
- 当前本地版本：`20260914-r7`

## 吸收更新

- 上游 `1acb468` / `20260914-r1`：订阅更新移除当前普通主节点时，自动切换到首个可用节点；不存在可用节点时设为 `nil`。本地在 `20260914-r7` 吸收，并增加回归测试。

## 忽略更新

- 上游 `3535ade` / `20260913-r1`：曾同步，后主动回退。上游在关闭 IPv6 时使用 DNS `prefer_ipv4` 并保留 IPv6 TUN 地址；本地 `c78187a` 恢复为 DNS `ipv4_only` 和仅 IPv4 TUN 地址，避免无 IPv6 出口节点的 DNS 与 TUN 初始化问题。
- 上游 `10393a5` / `20260913-r1`：仅调整 PO 文件的折行位置，无运行时、翻译内容或权限变化；不同步。
- 上游 `1acb468` / `20260914-r1`：版本号变更不直接同步。本地版本仅随实际修改递增，当前为 `20260914-r7`。

## 自主修改

- `20260914-r1`：资源更新改为独立维护，保留固定提交下载、完整性校验、规则集解码校验与失败保留。
- `20260914-r2`：将手动、定时与单资源更新范围分离；手动更新仅在 Dashboard 已安装时一并更新。
- `20260914-r3`：资源应用失败时返回真实回滚状态，避免将已回滚的更新显示为成功。
- `20260914-r4`：强化订阅解析、运行时端口初始化、reload 恢复和节点测速边界，并补齐回归测试。
- `20260914-r5`：保持通用 OpenWrt 兼容性，包括原生 procd 接口触发、动态上行 DNS 发现及 sing-box TUN 栈选择。
- `20260914-r6`：收敛 LuCI 生命周期和响应式 UI，保持原生 LuCI/Aurora 风格。
- `20260914-r7`：补齐客户端与服务端状态播报语义；吸收失效普通主节点回退修复。
- Dashboard 不随软件包预置；仅支持独立安装、更新与移除。
- URL 参数只解码一次，防止将 `/api?token=a%2Fb` 误改为 `/api?token=a/b`；覆盖 Host、密码及 HTTP、HTTP Upgrade、WebSocket 路径。
